#!/bin/sh
RET="OFF"

if [ $# -lt 1 ]; then
  echo "Usage: $(basename $0) header.h"
  exit 1
fi

CFILE=$(mktemp --suffix=.c)
echo "#include <$1>" > $CFILE
gcc -s -E $CFILE > /dev/null 2>&1

if [ $? -eq 0 ]; then
  RET="ON"
fi

rm -f $CFILE
echo $RET
exit 0
